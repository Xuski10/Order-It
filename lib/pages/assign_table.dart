import 'package:flutter/material.dart';

class AssignTable extends StatelessWidget {
  const AssignTable({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}